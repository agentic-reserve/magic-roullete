/**
 * Responsive design utilities
 * Exports all responsive layout modules
 */

export {
  getScreenDimensions,
  getScreenSizeCategory,
  scaleWidth,
  scaleHeight,
  scaleFontSize,
  moderateScale,
  getSpacing,
  getPadding,
  getBorderRadius,
  hasNotch,
  getSafeAreaInsets,
  BREAKPOINTS,
  isScreenSize,
  getResponsiveLayout,
  createResponsiveStyles,
  useResponsiveLayout,
} from './ResponsiveLayout';
